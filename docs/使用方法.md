# 使用方法

...

### 文件内容

#### `bin`二进制文件

包含原始的二进制指令码。

起始地址为`0x00`，与实际处理器的起始地址一致。

#### json文件

该文件包含二进制文件的base64编码以及必要的调试信息。

```json
{
    "bin": "...", // 二进制文件的base64形式
    "src": "...", // 源代码
    "lines": [...] // 二进制的每个地址对应的源代码行数，从0开始
}
```
